/**
 * HTTP Protocols - Interfaces para adaptadores HTTP
 *
 * @description
 * Centraliza todas as interfaces relacionadas a adaptadores HTTP,
 * facilitando a descoberta e manutenção dos contratos.
 *
 * @example
 * ```typescript
 * import { IHttpServer, IHttpRequest, IHttpResponse } from "@protocols/http";
 * ```
 */

export * from "./http-server.interface";
